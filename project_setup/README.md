# Project Setup

This script creates a new project folder with:
- `src/` for source code
- `tests/` for test files
- `docs/` for documentation

It also initializes Git and generates a README.md file.

Usage:
```bash
bash setup_project.sh my_project
